PROG  29: Create a database named "Company" in MongoDB and add a collection called "Employee" with the following fields: EmpId, EmpName, DOB, Salary and Designation.

> use Company
switched to db Company
> db.createCollection("Employee")
{ "ok" : 1 }
> db.Employee.insertOne({
...   EmpId: 1,
...   EmpName: "Suresh",
...   DOB: "1990-05-10",
...   Salary: 30000,
...   Designation: "Manager"
... })
{
	"acknowledged" : true,
	"insertedId" : ObjectId("69cb856f1cd701607a420387")
}

    a) Create multiple users with different roles.

> db.createUser({
   user: "writeUser",
   pwd: "123",
   roles: [
     { role: "readWrite", db: "Company" }
   ]
 })
Successfully added user: {
	"user" : "writeUser",
	"roles" : [
		{
			"role" : "readWrite",
			"db" : "Company"
		}
	]
}

> 
> db.createUser({
...   user: "readUser",
...   pwd: "read123",
...   roles: [{ role: "read", db: "Company" }]
... })
Successfully added user: {
	"user" : "readUser",
	"roles" : [
		{
			"role" : "read",
			"db" : "Company"
		}
	]
}

    b) Insert and update a document into the Employee collection while logged in as a read-write user.
> db.auth("writeUser", "123")
1

> db.Employee.insertOne({
...   EmpId: 2,
...   EmpName: "Anil",
...   DOB: "1995-03-12",
...   Salary: 25000,
...   Designation: "Clerk"
... })
{
	"acknowledged" : true,
	"insertedId" : ObjectId("69cb866c1cd701607a420388")
}

> db.Employee.updateOne(
...   { EmpId: 2 },
...   { $set: { Salary: 26000 } }
... )
{ "acknowledged" : true, "matchedCount" : 1, "modifiedCount" : 1 }
>